[package]
name = "bundle_cart_transform"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
shopify_function = "1.1.0"

[dev-dependencies]
serde_json = "1.0"

[profile.release]
lto = true
opt-level = 'z'
strip = true
