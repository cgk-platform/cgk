# Google Workspace skill — no API keys needed here.
# Authentication uses per-profile OAuth credentials stored at:
#   <workspace_root>/credentials/google-workspace-oauth.json
#
# Run setup: uv run scripts/setup_oauth.py
